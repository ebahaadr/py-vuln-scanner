# py-vuln-scanner

Simple Python-based port & vulnerability scanner.

## Features

- Multi-threaded TCP port scanning
- Common service detection (FTP, SSH, HTTP, HTTPS, etc.)
- Basic banner grabbing
- Demo vulnerability signatures (vsftpd, old OpenSSH versions)

## Usage

```bash
python main.py